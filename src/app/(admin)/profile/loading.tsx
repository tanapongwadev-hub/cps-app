/**
 * Profile Loading State
 */

import { LoadingSpinner } from "@/components/feedback/loading-skeleton";

export default function ProfileLoading() {
  return <LoadingSpinner text="กำลังโหลดโปรไฟล์..." />;
}
